"""Full sequence model: input/output embeddings around a stack of GDN blocks.

Configured by ``configs/model/gdn.yaml``; the ``backend`` field selects the
sequence-mixing backend per ``iccl.models.ops``.
"""
