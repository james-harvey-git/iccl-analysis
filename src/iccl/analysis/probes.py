"""Probing tools for captured activations and memory states.

First target mirrors the source paper's key finding: linear decodability of the
task vector (which primitives are active) from hidden states, extended here to
how that code evolves across the in-context task sequence.
"""
